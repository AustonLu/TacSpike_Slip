# Training Scripts

Training entry points live here.

- `train_lite_scnn.py`: train the main LIF SCNN with 20 ms strict sliding-window inputs.
- `run_ablation.py`: planned future launcher for polarity, pooling, window, readout, smoothing, imbalance, and width ablations.
