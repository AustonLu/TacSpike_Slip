# Validation Scripts

Validation and sanity-check scripts live here.

- `stage1_check_dataset.py`: checks HDF5 release schema, split sizes, random sample shapes, and `event_count` alignment.
- `stage1_inspect_sample.py`: renders one selected or random HDF5 window for visual sanity checks.

