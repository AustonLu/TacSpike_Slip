# Training Scripts

Training entry points will live here.

Planned stage 2 scripts:

- `train_lite_scnn.py`: train the main LIF SCNN with 20 ms strict sliding-window inputs.
- `run_ablation.py`: launch polarity, pooling, window, readout, smoothing, imbalance, and width ablations.

